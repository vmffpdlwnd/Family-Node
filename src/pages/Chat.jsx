import React from 'react';
import ChatWidget from '../components/ChatWidget';

const Chat = () => {
  return (
    <div style={{ padding: 0 }}>
      <ChatWidget pageMode />
    </div>
  );
};

export default Chat;
